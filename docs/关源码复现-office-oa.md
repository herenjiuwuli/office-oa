# 关掉源码，你能写出来吗？（office-oa 复现练习）

> 衡量标准（你定的）：关掉源码能自己写出来 = 懂；写不出 = 没懂。
> 方法（拆解循环）：① 跑起来当使用者 → ② 三问拆（**干嘛 / 为什么 / 删了会怎样**）→ ③ 关源码重写 → ④ diff 对照 → 差距记错题本。
>
> ⚠️ 说明：这个项目是「你定需求 + AI 实现」协作产出。所以这份练习的**唯一目的**就是把「AI 写的」变成「你能讲的」。练完 16 题，这个项目才敢往简历上写。

## 📊 我的进度（随时更新）

| 题 | 状态 |
|---|---|
| **2. 审批引擎六步（硬门槛）** | ✅ **2026-09-19 已通过**（用「攻击者 6 问」推出五步，对照代码确认一致） |
| **3. 流程快照** | ✅ **2026-09-19 已通过**（对照 `submitRequest()`；快照 6 字段：step_no / name / approver_type / approver_ref / mode / dept_scoped） |
| **4. 并发防线** | ✅ **2026-09-19 已通过**（讲解后自己复述正确：null = 还没批 → 有人批了 action 就变 → 后人 WHERE 不成立、写不进去） |
| 其余 13 题 | ⬜ |

> 💡 已验证有效的通关方法：**不从代码入手**。先答「我故意搞事，系统会怎么反应」那 6 个场景题（授权/状态/身份/撞车/接力/顺序）→ 猜完再打开 `engine.js` 对照 → 六步自己就浮现了。
> 我答错的地方：**已归档的单再批 → 我猜"成功"，实际必须 409 拒绝**（否则污染审计、重复触发下游）；**并发撞车 → 我猜"可能卡顿"，实际是"一个成功一个 409"**。

## 练习清单（每题：关掉源码，在空白编辑器/纸上写，再对照真实代码 diff）

1. **状态机**：画出 `requests.status` 的全部流转（`draft / pending / approved / rejected / cancelled`），标出「哪个状态能提交、哪个能撤回、哪个是终态」，以及驳回重提时 `round` 怎么变。
   → 对照 `server/flow/engine.js` 顶部 `SUBMITTABLE` / `CANCELLABLE` + README「状态机」一节。

2. **⭐⭐ 审批引擎六步**：白纸写出 `actOnRequest()` 的六步**顺序**，并说出「为什么必须是这个顺序」。
   → 对照 `server/flow/engine.js` 的 `actOnRequest()`（本项目最核心的一道题，**答不出来就等于这项目没懂**）。

3. **流程快照**：写出 `submitRequest()` 里快照存了哪些字段、为什么审批时**不重新查流程模板**。
   → 对照 `submitRequest()` 的 `snapshot` 构造 + `parseSnapshot()`。

4. **并发防线**：写出防并发那一句 SQL，以及为什么用 `changes` 而不是应用层加锁。
   → 对照 `actOnRequest()` 第 ④ 步。

5. **鉴权**：写出 `signToken` / `verifyToken` 的 HS256 流程（含防时序、`exp` 校验），以及密码为什么用 `scrypt` + 加盐。
   → 对照 `server/auth.js`（纯函数，不碰库）。

6. **权限模型**：写出 `guards.js` 每一层做了什么、为什么权限**不缓存进 token**。
   → 对照 `server/guards.js` + `server/permissions.js`。

7. **横向越权防线**：写出 `canViewRequest()` 的三个放行条件，并说明「三个都不满足返回什么码、为什么」。
   → 对照 `server/flow/engine.js` 的 `canViewRequest()`。

8. **审批人解析**：写出 `resolveApprovers()` 的 3 个分支（`user` / `role` / `manager`），并解释为什么解析失败**必须抛 409 而不是 500**。
   → 对照 `resolveApprovers()`（注意 `manager` 分支里「上级为空」那段注释）。

9. **或签 / 会签结算**：写出「本步是否通过」的判定表达式，以及驳回后为什么要 `closeRemainingTasks()`。
   → 对照 `actOnRequest()` 第 ⑤ 步的 `passed` 表达式。

10. **⭐ 登出强制作废（M2 新增）**：白纸写出这条链的三处改动 —— ① 签发时给 token 加什么字段（`jti`，怎么生成）② 登出时往哪张表写什么 ③ 守卫在哪一步查、命中返回什么码。**并说出「为什么按 `jti` 作废，而不是把这个用户所有 token 都作废」。**
    → 对照 `server/auth.js`（`signToken`）+ `server/tokenBlacklist.js` + `server/guards.js`。
    ⭐ 加分点：说得出「JWT 无状态 = 登出其实什么也没发生，服务端不知道你登出过」这个前提。

11. **⭐ 附件上传安全五面（M2 新增）**：白纸列出「① 类型 ② 大小/数量 ③ 落盘名 ④ 下载 ⑤ 响应头」五条防线各自怎么做的，**并各写一句「不这么做会被怎么打」**。
    → 对照 `server/lib/storage.js` + `server/routes/attachments.js`。
    ⭐ 加分点：说得出「路径穿越这条，即使上游解析器已经剥了路径，我也没把安全建立在它身上」——**纵深防御 = 不依赖上游做了什么**。

12. **⭐⭐ 附件数量上限的并发正确性（第二轮用例抓出来的真缺陷）**：白纸写出「为什么『先 `SELECT COUNT(*)` 再 `INSERT`』在并发下会失效」，并写出**修好之后那一条 SQL 长什么样**、失败时靠什么判定、以及失败后还要做一件什么收尾的事。
    → 对照 `server/routes/attachments.js` 上传分支的 `INSERT ... SELECT ... WHERE (…) < ?`。
    ⭐ 加分点：说得出① 中间隔着 `await part.toBuffer()`（读请求体必然要 await）所以检查与写入不在同一时刻；② 判定收敛到 `changes === 0`，和「并发审批查 `changes`」是同一套路子；③ 失败必须 `deleteUpload()` 回收刚落盘的文件，否则留孤儿。
    ⭐⭐ **能说出这条检查项的普适形式**：**凡是「先查一次再写一次」的写法，都要多问一句「并发下还成立吗」。**

13. **⭐ 导出 CSV 的三条安全线（M4 新增）**：白纸写出 ① 「CSV 公式注入」怎么防（**包含**「先排除合法数字」和「先加前缀再引号包裹」这两个细节），② 为什么「导出的可见性」必须和列表**共用一处**判断（不共用会出什么样的问题），③ 文件名和「导出了几条」为什么都由服务端给、前端不去数 CSV 行。
    → 对照 `server/lib/csv.js` + `server/routes/requests.js` 的 `scopeFilter()` 与导出分支。
    ⭐ 加分点：说得出「导出不是搬运数据，是**把数据变成一个要在别人机器上被打开的文件**」——公式注入的受害者是**打开文件的人**，不是系统本身。
    ⭐⭐ 附加题（考「闭环」）：说出这一轮**平台为什么不够用**、补了哪两处能力（响应头断言 / 绕开 `res.text()` 按字节解码），以及「BOM 为什么会被 `res.text()` 悄悄吃掉」。

14. **⭐ 会议室冲突防线（M5 新增）**：白纸写出 ① 时间怎么折算成槽（`08:00` → 槽几）、② `room_slots` 的**主键**是什么、它凭什么能挡住并发、③ 整件事为什么必须包在 `BEGIN IMMEDIATE` 事务里、④ 撞了唯一约束之后回什么码、为什么还要**反查一条预订**出来。
    → 对照 `server/routes/meetings.js` + `server/schema.sql` 的 `room_slots`。
    ⭐ 加分点：说得出**为什么折槽、而不是存 `start/end` 比大小** —— 区间重叠（`a.start < b.end AND b.start < a.end`）是**应用层逻辑**，并发下照样漏；折成离散槽之后，「重叠」变成「**同一个槽被插两次**」，正好是唯一约束能表达的语义。
    ⭐⭐ 普适形式：**能下沉到数据库约束的规则，就不要留在应用层。**（这题不是「又踩了一次 TOCTOU」，而是**把第 12 题那次教训主动迁移过来** —— 这才是「经验」和「记住了个名词」的区别。）

15. **⭐⭐ 统计接口的范围权限（M6 新增）**：白纸写出 ① `scope` 的三个取值（`mine` / `dept` / `all`）分别对应谁、② 为什么**复用 `request:read:all`** 而不新开一个 `stats:read`、③ 越界时回什么码、**为什么不能静默降级到 `mine`**、④ `maxScopeFor()` 判定三档的依据。
    → 对照 `server/routes/stats.js` + `server/routes/attendance.js`（两处**同一套**语义：考勤也复用它）。
    ⭐ 加分点：说得出「单据详情的越权面是**一行**，统计的越权面是**一批**」 —— 不收敛范围的聚合接口会被当字典查，拼出「谁提了多少单、谁总被驳回」的组织画像。
    ⭐⭐ 附加题（考「闭环」，这题的答案比第 13 题更狠）：说出这一轮**平台又不够用** —— 执行器**发不出 query string**；更隐蔽的是 **`test_cases` 表根本没有 `query_json` 这一列**，所以用例里写 `?scope=all` 是**入库即丢**，服务器收不到参数、按 `maxScope` 回 `200`，而用例断言的正好也是 `200` → **全绿**。说出那句收口：**一个能力要真落地，得改「表结构 → 持久化 → 执行器」三层，漏一层就是假绿。**

16. **⭐⭐ 考勤缺卡的算法 + 它翻过的车（M7 新增）**：白纸写出 ① 为什么**缺卡不落行**、② 缺卡的算式（「**已经过去的**工作日 − 有上班卡的天数」）、③ 「已经过去」在**当前月 / 过去月 / 未来月**三种情况分别截到哪一天、④ 重复打卡为什么是 `409` 而不是应用层「先查再插」、⑤ 下班先于上班回什么码。
    → 对照 `server/routes/attendance.js` 的 `elapsedWorkingDays()` + `clock()` 分支。
    ⭐ 加分点：说得出**第一版错在哪** —— 分母用了「**当月**工作日」，月中打开看板时 `9/20` 之后**还没到**的日子也被算成应出勤，页面上写着「全公司缺卡 **163** 天」。修法：过去月 = 整月 / 当前月 = 截至今天 / 未来月 = 0（本月应出勤 22 → **14**、缺卡 163 → **99**）。
    ⭐⭐ **一定要说清「为什么测试抓不到它」**：接口 `200`、结构完整、分项之和也自洽 —— **它只是数字荒谬**。当时 259 条接口用例 + 72 条真机断言 + 84 条平台用例**全绿**，没有任何断言会红。**是靠重拍导览截图、把页面打开看了一眼才发现的。**
    ⭐⭐ 普适形式：**看到「总数 / 应出勤 / 应完成」这类做分母的数字，先问一句「这个时间范围到今天为止了吗」。**

## 我的示范轮（以第 2 题为例）

> 我做给你看「能写出来长什么样」。下面是**标准答案**（可直接拿去跟你的默写做 diff）：

`actOnRequest(requestId, userId, action, comment)` —— action 只允许 `approve` / `reject`：

| 步 | 做什么 | 失败码 | 为什么在这一步 |
|---|---|---|---|
| ① | **授权**：①-1 申请人不能审自己的单（防自批）；①-2 你至少是这张单某个环节的审批人 | 403 | **必须最先做** —— 先返 409 会让无关人员靠状态码探测单据状态（信息泄漏） |
| ② | **状态校验**：`status !== 'pending'` → 拒 | 409 | 授权已过，这里才谈「现在能不能审」 |
| ③ | **身份细分**：查 `approval_tasks(request_id, step_no=current_step, round, approver_id)`，查不到 → 拒 | 403 | 「是审批人」≠「是**这一步**的审批人」 |
| ④ | **条件更新**：`UPDATE ... WHERE id = ? AND action IS NULL`，`changes === 0` → 拒 | 409 | ★ 并发防线，靠 DB 原子性 |
| ⑤ | **结算该步**：`passed = !hasReject && (mode === 'any' ? anyApprove : allDone)` | — | 或签 vs 会签的分岔点 |
| ⑥ | **推进 or 归档**：有 `reject` → 驳回 + 关掉本步剩余任务；`passed` → 有下一步则推进 `current_step` + 分配任务，没有则 `approved` 归档；`passed === false`（会签没集齐）→ **什么都不做，保持 pending** | — | 全部包在一个事务里，配置错误 → ROLLBACK，不留半截状态 |

对照真实代码（`server/flow/engine.js`）：**一致 ✅**。说明这题能写出来，面试可放心讲。

## 评分标准

- 每题：能独立写出核心逻辑 = ✅；卡住或写错 = ❌，回到对应源码重读 + 记错题本。
- **第 2 题（六步）是硬门槛**：这题 ❌ 就别把 OA 写进简历的技术亮点里，先补。
- **第 10、11 题（M2 两个安全点）是目前最「新鲜」的素材** —— 面试弹药里第 23～27 题都从这里出，练完能扛住安全方向的追问。
- **第 12 题（并发上限）是唯一一道「真缺陷」题**：它不是设计题，是用例抓出来的 bug。练它不是为了记住 SQL，而是为了长出那个反射 ——**看见「先查再写」就想到并发**。
- **第 13 题（导出安全，M4）** 是「安全设计」里最新的一块，带一道**闭环附加题** —— 面试弹药第 33、34 题从这里出。
- **第 14 题（会议室，M5）是唯一一道「把教训迁移过去」的题**：它的价值不在那个主键，而在「第 12 题教会我的东西，我在下一个模块**主动**用上了」。
- **第 15 题（统计范围，M6）带第二道闭环附加题** —— 面试弹药第 35、36 题从这里出。这道的附加题比第 13 题更狠：它考的是「**只改执行器没用**」。
- ⭐⭐ **第 16 题（考勤口径，M7）是本项目唯一一道「测试全绿也抓不到、靠肉眼发现」的题** —— 面试弹药第 29、37 题从这里出。**它比第 12 题更值钱**：第 12 题至少还有用例会红，第 16 题**一条都不会红**。能把这题讲透（尤其是「为什么三层测试都抓不到」），「测试的层次决定你能看见什么层次的缺陷」这句话才不是背的。
- 16 题全 ✅ → 这个项目你敢讲、扛得住深挖，可以投技术岗。
- 有 ❌ → 先补那一块，别急着投。

## 错题本模板

| 题号 | 我写错的 / 卡住的 | 正确做法 | 下次怎么记住 |
|---|---|---|---|
| 2 | 「已归档的单再批」猜成"成功" | 必须 409 拒绝（否则污染审计、重复触发下游） | 已经结束的东西不能再改 |
| 2 | 「并发撞车」猜成"可能卡顿" | 一个成功、一个 409（条件更新裁决） | 撞车的结果是"分胜负"，不是"快慢" |
| 3 | "不做快照最糟的事"答成"卡顿" | 是**流程混乱 / 合法性断裂**（在途单被加塞、审批痕迹对不上） | 数据会变 + 历史要留痕 → 先想"乱"，不是"慢" |
| 3 | 快照时点答成"展示时" | **提交时**（那一刻定格"法律版本"） | 提交 = 签合同；之后模板改动只影响新单 |
| 4 | 把 `action IS NULL` 理解成"已被批过"（**反了**） | `action IS NULL` = **还没批**；批过之后变成 'approve' / 'reject' | **null = 空白 = 还没人填** |
| 4 | 不知道 `changes` 从哪来 | 数据库执行后**自动返回**的"影响行数"，不用自己定义 | 数据库报数，不是我算 |
| | | | |
