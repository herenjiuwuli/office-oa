node_modules/
dist/
test-results/
coverage/

# Playwright 产物
playwright-report/
blob-report/
.playwright/

# 本地数据库不进仓库（可能含真实数据，见 README 安全红线）
data/*.db
data/*.db-journal
data/*.db-wal
data/*.db-shm

# 清库前的备份也不进仓库
data/backups/

# 附件上传目录（用户上传的文件不进仓库）
data/uploads/
data/e2e-uploads/

# 本地环境变量
.env
.env.local

*.log
